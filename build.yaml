targets:
  $default:
    builders:
      source_gen:combining_builder:
        options:
          ignore_for_file:
            # https://github.com/google/json_serializable.dart/issues/625
            - unnecessary_cast
